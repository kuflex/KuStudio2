import QtQuick 2.3
import QtQuick.Controls 1.2

import com.kuflex.GuiBridge 1.0


ListModel{
    /*Component.onCompleted: {
            var count=myWidget.countModules();
            for(var i=0; i<count-1; i++){
               append({"name":myWidget.setNewModule()[i], "type":myWidget.setNewModule()[i+1]});
            }
        }*/
}
